{
  "_important": "同为 AWS S3 的多个区域、账号或 Bucket 必须使用多个独立 JSON 文件。",
  "_unique_rules": [
    "name 必须不同，例如：AWS-香港、AWS-东京。",
    "rclone.remote 必须不同，例如：aws_hk、aws_tokyo。",
    "环境变量前缀必须对应 remote：aws_hk 对应 RCLONE_CONFIG_AWS_HK_*。",
    "不同区域必须分别修改 REGION，例如 ap-east-1、ap-northeast-1。",
    "不同账号分别修改 ACCESS_KEY_ID 和 SECRET_ACCESS_KEY；Bucket 和目录填写在 remote_path。"
  ],
  "name": "AWS S3 示例目的地",
  "type": "awss3",
  "remote_path": "your-bucket/backups/project-a",
  "rclone": {
    "remote": "aws_s3_backup",
    "env": {
      "RCLONE_CONFIG_AWS_S3_BACKUP_TYPE": "s3",
      "RCLONE_CONFIG_AWS_S3_BACKUP_PROVIDER": "AWS",
      "RCLONE_CONFIG_AWS_S3_BACKUP_ACCESS_KEY_ID": "your_aws_access_key_id",
      "RCLONE_CONFIG_AWS_S3_BACKUP_SECRET_ACCESS_KEY": "your_aws_secret_access_key",
      "RCLONE_CONFIG_AWS_S3_BACKUP_REGION": "ap-east-1",
      "RCLONE_CONFIG_AWS_S3_BACKUP_ACL": "private"
    },
    "flags": []
  }
}
